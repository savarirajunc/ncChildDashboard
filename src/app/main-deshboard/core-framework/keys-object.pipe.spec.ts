import { KeysObjectPipe } from './keys-object.pipe';

describe('KeysObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new KeysObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
