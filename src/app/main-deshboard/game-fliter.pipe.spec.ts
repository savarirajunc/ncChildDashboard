import { GameFliterPipe } from './game-fliter.pipe';

describe('GameFliterPipe', () => {
  it('create an instance', () => {
    const pipe = new GameFliterPipe();
    expect(pipe).toBeTruthy();
  });
});
