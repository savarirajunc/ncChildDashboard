import { ChildFilterPipe } from './child-filter.pipe';

describe('ChildFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ChildFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
