import { StanderFilterPipe } from './stander-filter.pipe';

describe('StanderFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StanderFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
