import { SchoolFilterPipe } from './school-filter.pipe';

describe('SchoolFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SchoolFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
